<?xml version="1.0" encoding="utf-8" ?>
<local:BasePage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:View.Pages"
                xmlns:vm="clr-namespace:ViewModel.ViewModels;assembly=ViewModel"
                Title="유저 자산 랭킹"
                x:DataType="vm:RankingViewModel"
             x:Class="View.Pages.RankingPage">
    
    <local:BasePage.Resources>
        <Style x:Key="Label" TargetType="Label">
            <Setter Property="FontSize" Value="Micro" />
            <Setter Property="HorizontalOptions" Value="Center"/>
        </Style>
    </local:BasePage.Resources>

    <CollectionView ItemsSource="{Binding Ranks}" Margin="16">
        <CollectionView.ItemsLayout>
            <LinearItemsLayout Orientation="Vertical" ItemSpacing="8" />
        </CollectionView.ItemsLayout>
        
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="vm:RankModel">
                <HorizontalStackLayout Spacing="8">
                    <Label Style="{StaticResource Label}" Text="{Binding UserId}"/>
                    <Label Style="{StaticResource Label}" Text="{Binding Asset, StringFormat='{0:N0}'}"/>
                </HorizontalStackLayout>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
    
</local:BasePage>